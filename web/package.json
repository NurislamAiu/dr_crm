{
  "name": "crm-web",
  "version": "0.1.0",
  "private": true,
  "description": "Собственный интерфейс чатов CRM поверх Wazzup User API v3 (WhatsApp QR)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test dist",
    "lint": "eslint . || echo 'eslint not configured yet'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
